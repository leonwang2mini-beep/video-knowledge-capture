# AGENTS.md

## 项目定位

- 项目名称：视频知识捕手
- 项目 ID：`P0004`
- 项目类型：`automation`
- 主领域：`edu-education`
- 当前阶段：`build`
- 项目目标：帮助用户将在公开视频平台发现的内容，通过 Agent Skill 或本地入口自动整理并写入个人 Obsidian 知识库。

## 任务开始前

- 先读取项目结构、`PROJECT.yaml`、本文件、`MEMORY.md` 和当前任务相关文档，再修改文件。
- `PROJECT.yaml` 是项目身份、分类、当前里程碑与验收证据的唯一结构化来源。
- 发现文档与代码、测试或实际产物冲突时，以可验证证据为准，并明确记录冲突。
- 不确定信息必须标注为 `已验证`、`来自记忆` 或 `我的推断`。

## 工作规则

- 默认使用中文沟通；代码、命令、变量名和文件名使用英文。
- 结论先行，说明权衡、建议，以及对用户、业务和维护者的影响。
- 保持最小改动，不进行与当前目标无关的重构。
- 修改前说明计划修改的文件及原因；修改后执行与风险相称的验证。
- 项目状态必须引用真实证据；目录名、文件修改时间和待办列表不能单独证明进展。
- 未经明确授权，不部署、不发布、不发送外部消息、不删除数据、不修改远端配置。
- 不读取、输出、记录或提交 `.env`、API Key、Token、Cookie、私钥、证书等凭据值。

## MEMORY.md 规则

- 开始工作前完整读取 `MEMORY.md`。
- 只记录稳定约定、架构决策、验证过的命令、踩坑、外部资源位置和后续事项。
- 不把代码可直接查到的普通事实、临时扫描结果、健康度、优先级或 AI 建议写入 `MEMORY.md`。
- 凭据只记录存放位置和用途，不记录值。

## 验证与收尾

- 报告改动文件、修改原因，以及对用户、业务和维护者的影响。
- 列出实际执行的验证命令、结果和未验证项。
- 说明是否更新 `MEMORY.md`；若更新，最后回显：`本次新增到 MEMORY.md：...`

# Security Policy

## 适用范围

本策略适用于本仓库维护的 canonical Agent Skill、安装脚本、Windows
loopback 服务、Web/CLI/Hermes 入口、下载与转写编排、任务台账和
Obsidian 写入逻辑。

项目处理的敏感资产包括用户显式配置的知识库路径、下载媒体、转写内容、
临时任务文件、Windows DPAPI 保护的可选登录态，以及第三方运行时文件。

当前社区预览期只评估最新 `main` 和最新发布版本中的安全问题。旧版本和
第三方分支不保证获得安全修复。

## 威胁模型与信任边界

以下输入均视为不可信：

- 用户提交的公开 URL、重定向地址、网页 HTML 和平台返回数据；
- 下载器、浏览器适配器和第三方运行时产生的输出；
- 上传文件名、媒体元数据、Hermes 参数和本地 HTTP 请求；
- 与本地服务处于同一台电脑上的其他进程。

项目假定 Windows 当前用户和用户显式配置的 Inbox 目录可信，但不假定
浏览器页面、视频平台、下载内容或同用户进程天然可信。

## 安全不变量

- 服务只能监听 `127.0.0.1`，不得暴露到局域网或公网。
- 浏览器发起的写操作必须经过 Origin 检查；跨来源写请求必须拒绝。
- 公共页面访问必须逐跳验证协议、端口、DNS 结果和重定向目标，阻断本机、
  私网、链路本地和保留地址。
- URL 不得携带用户名、密码、Cookie、Token 或其他凭据。
- 外部进程必须使用参数数组和 `shell: false`，不得把不可信文本拼接为命令。
- 下载、临时文件、媒体保留和笔记写入必须限制在显式配置或受管目录内，
  并阻断路径穿越和符号链接越界。
- 不导入现有浏览器资料或 Cookie。可选登录态只能在隔离流程中取得，
  明文只在内存中使用，持久化时使用 Windows DPAPI 当前用户保护。
- 第三方运行时必须来自声明来源并在执行前验证固定摘要。
- 日志、HTTP 回执和错误台账不得包含 Cookie、Token、解密键、原始凭据
  或不受限的第三方输出。
- 平台不支持、身份验证失败或完整性校验失败时必须明确失败，不得绕过
  DRM、付费、私有、地区或账户访问控制。

## 应报告的问题

包括但不限于：

- SSRF、重定向或 DNS 重新绑定绕过；
- 任意命令执行、代码执行或参数注入；
- 路径穿越、越界文件写入或删除；
- Cookie、Token、DPAPI 明文或其他凭据泄漏；
- Origin、loopback 或运行时完整性检查绕过；
- 未经授权写入用户知识库；
- 可从不可信平台内容触发的持久化脚本或 HTML 注入；
- 安装器通过符号链接覆盖其他 Skill、插件或用户文件；
- 能跨越已声明信任边界并产生现实影响的拒绝服务问题。

## 不在安全报告范围内

- 视频平台接口变化、公开链接失效或平台主动封禁；
- 转写准确率、摘要质量和知识内容评价；
- 项目已经明确拒绝的私有、付费、DRM 或需要登录的内容；
- 只能在攻击者已经完全控制 Windows 当前用户后实现的行为；
- 与本项目集成路径无关、不可从本项目触达的第三方漏洞；
- 修改过的分支、被替换的运行时或用户主动关闭安全检查后的问题。

请勿通过安全报告请求增加平台绕过、Cookie 导入或访问受限内容的能力。

## 已知限制

- Loopback 和 Origin 检查可以限制网络及浏览器来源，但不能对已经运行在
  同一 Windows 用户权限下的恶意本地进程提供强身份认证。
- Windows DPAPI 保护不能抵御已经控制当前 Windows 用户会话的攻击者。
- 平台适配依赖第三方网站和固定版本运行时，其可用性可能随外部变化。
- 当前本地执行核心只支持 Windows；安装 Skill 不代表其他操作系统已经
  获得运行时支持。

## 私密报告方式

优先使用 GitHub 仓库的 **Security → Advisories → Report a vulnerability**
提交私密报告。仓库公开时，维护者应启用 GitHub Private Vulnerability
Reporting。

如果该入口尚未启用，可以创建一个不包含漏洞细节的普通 Issue，请求维护者
提供私密联系渠道。不要在公开 Issue、Discussion、Pull Request 或社交媒体中
公开漏洞细节。

报告应包含：

- 受影响版本或提交；
- 最小化、脱敏的复现步骤；
- 实际影响和所跨越的安全边界；
- 必要的日志或证明，但不得包含真实凭据和个人数据；
- 已知缓解措施。

维护者将尽力确认、评估和协调修复，但社区预览版暂不承诺固定响应或修复
时限。请在修复发布或双方约定日期之前保持私密披露。
